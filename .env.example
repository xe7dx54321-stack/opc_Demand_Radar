# Optional. Used by future LLM extraction/scoring loops.
OPENAI_API_KEY=

# Optional. Override default project paths if needed.
DEMAND_RADAR_CONFIG_DIR=configs
DEMAND_RADAR_DATA_DIR=data
DEMAND_RADAR_OUTPUT_DIR=outputs

