# Optional. Used by future LLM extraction/scoring loops.
OPENAI_API_KEY=

# Optional. Override default project paths if needed.
DEMAND_RADAR_CONFIG_DIR=configs
DEMAND_RADAR_DATA_DIR=data
DEMAND_RADAR_OUTPUT_DIR=outputs

# Demand Radar LLM semantic merge settings (Stage 2.9)
# Set these in a local .env file. Never commit real keys.
DEMAND_RADAR_LLM_BASE_URL=
DEMAND_RADAR_LLM_API_KEY=

# Optional: override model from env instead of config
DEMAND_RADAR_LLM_MODEL=
